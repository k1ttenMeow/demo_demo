spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    # 关键：数据库名必须为 community_chronic（与你的库名完全一致）
    url: jdbc:mysql://localhost:3306/community_chronic?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 12345678
# 强制后端端口为 8081（与前端 8080 完全隔离）
server:
  port: 8081